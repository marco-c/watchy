<ul>
</ul>

<div id="show-list-outer">
  <ul id="show-list">
{{#each model as |show|}}
    {{#link-to 'show' show.id}}
    <li>
      <img src="{{show.image}}">
      <h3>{{show.title}}</h3>
      <p>{{show.overview}}</p>
    </li>
    {{/link-to}}
{{/each}}
  </ul>
</div>

{{outlet}}
